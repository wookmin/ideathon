{
  "name": "meomchit-ait",
  "private": true,
  "type": "module",
  "scripts": {
    "build:flutter": "node scripts/build-flutter-web.mjs",
    "build:bridge": "vite build --config vite.bridge.config.ts",
    "assemble": "node scripts/assemble.mjs",
    "build": "npm run build:flutter && npm run build:bridge && npm run assemble && ait build",
    "deploy": "ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^3.4.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.0.0"
  }
}
